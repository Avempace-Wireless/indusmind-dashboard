# API Configuration
VITE_API_BASE_URL=http://localhost:4000
VITE_WS_BASE_URL=ws://localhost:4000

# Application
VITE_APP_NAME=IndusMind Energy Dashboard
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=development

# Feature Flags
# Options: "api" (real API only), "mock" (mock data only), "hybrid" (API with mock fallback)
VITE_DATA_MODE=hybrid
VITE_ENABLE_OFFLINE_MODE=false
VITE_ENABLE_PWA=false

# Production: Use Railway backend URL
# VITE_API_BASE_URL=https://indusmind-backend-production.up.railway.app/api
